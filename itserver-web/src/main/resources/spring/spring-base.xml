<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
    <bean id="propertyConfigurer" class="com.longfor.itserver.spring.configurer.JoddPropsConfigurer">
        <property name="locations">
            <list>
                <value>classpath:init.properties</value>
            </list>
        </property>
        <property name="fileEncoding" value="UTF-8"/>
    </bean>

    <bean id="edsHelper" class="com.longfor.eds.helper.EDSHelper">
        <!--EDS服务器地址-->
        <property name="serverUrl" value="${eds.url}"/>
        <!--在EDS注册后分配给你的TOKEN-->
        <property name="token" value="${eds.token}"/>
    </bean>
    <bean id="adsHelper" class="com.longfor.ads.helper.ADSHelper">
        <!--ADS服务器地址-->
        <property name="serverUrl" value="${ads.url}"/>
        <!--在ADS注册后分配给你的TOKEN-->
        <property name="token" value="${ads.token}"/>
    </bean>
</beans>